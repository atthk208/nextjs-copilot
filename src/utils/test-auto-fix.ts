// Test file with formatting issues that can be auto-fixed
export const greet = (name: string) => {
  console.log(`Hello, ${name}!`);
  return `Greeting: ${name}`;
};

export function calculate(a: number, b: number) {
  const result = a + b;
  return result;
}
